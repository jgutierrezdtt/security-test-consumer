name: Test Reusable Call (Hello)
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  hello:
    uses: jgutierrezdtt/security-platform/.github/workflows/reusable/test-hello.yml@main
    with:
      message: "Hello from security-test-consumer!"
    secrets: inherit
